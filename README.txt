The batch file "compsetup.bat" is a script that will automatically setup most aspects of a new machine.
Runs installers, sets up printers, changes preferences and settings, etc.
Works for desktop workstations as well as Surfaces and Laptops

These scripts must be run as an administrator. (Right click -> Run as administrator)